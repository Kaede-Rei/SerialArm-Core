# SerialArm Python Binding

Python bindings for the SerialArm C++ control, dynamics and hardware core

The wheel is built from this directory with scikit-build-core and uses the repository-level Conan 2 dependency environment for Pinocchio, Eigen and yaml-cpp

See the repository `README.md`, `Tutorial.md` and `API.md` for installation, Robot Profile and runtime usage
